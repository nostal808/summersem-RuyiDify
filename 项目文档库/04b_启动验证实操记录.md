# 04b · Docker Compose 启动验证实操记录

- last_verified: 2026-07-15
- scope: 真实执行 `docker compose up -d` 于 `C:\ai\RuyiDify\docker`，并完成访问验证
- evidence: ✅ 真实运行验证（命令输出来自本次执行）
- 关联: `04_启动方式报告.md`（理论盘点）、`00_AI优先查阅说明.md`

> 本记录是「方式 A：Docker Compose 整体启动」的**真实执行版**，覆盖 6 步。结论：**启动成功，可访问。**

## 步骤 1 · 启动前检查（结果）

| 检查项 | 结果 |
|--------|------|
| Docker Desktop 运行 | 初始**未运行**（daemon 连接失败 `npipe:////./pipe/dockerDesktopLinuxEngine`）；已启动 `Docker Desktop.exe`，随后 `docker info` 正常，Server 29.6.1 |
| `docker version` | Client 29.6.1 / Server 29.6.1 ✅ |
| `docker compose version` | v5.2.0 ✅ |
| 端口 80 / 443 本地监听 | **空闲**（无 `0.0.0.0/127.0.0.1:80|443` 监听）✅ 无冲突 |
| `docker compose config --quiet` | 首次因缺 `.env` 报错；建 `.env` 后 `CONFIG OK` ✅ |
| 注册镜像源 | daemon 已配置多个 mirror（含 `docker.jiaxin.site` 等），首拉走加速 |

## 步骤 2 · 配置文件

- `docker/.env` **此前不存在** → 按规则从 `.env.example` **复制生成**（不覆盖，因无原文件）。
- **未修改**任何数据库、向量库、密码、端口、镜像版本，全部使用项目默认。
- 安全：未打印 `.env` 内容，未读取/输出任何密钥/Token/密码。

## 步骤 3 · 启动服务

```bash
cd C:\ai\RuyiDify\docker
docker compose up -d
```
- 退出码 **0**，所有容器创建并启动。
- 首次拉取镜像：8 个镜像中 7 个很快就绪，`weaviate:1.27.0` 层数多、经 mirror 拉取较慢（约 10+ 分钟），期间持续观察，未因短暂无输出重复启动。

## 步骤 4 · 检查状态（`docker compose ps`）

| 容器 | 镜像 | 状态 | 端口 | 备注 |
|------|------|------|------|------|
| docker-nginx-1 | nginx:latest | Up 2m | 0.0.0.0:80→80, 0.0.0.0:443→443 | 反向代理 ✅ |
| docker-web-1 | langgenius/dify-web:1.15.0 | Up 3m | 3000/tcp | 前端 ✅ |
| docker-api-1 | langgenius/dify-api:1.15.0 | **Up 2m (healthy)** | 5001/tcp | MODE=api ✅ |
| docker-worker-1 | langgenius/dify-api:1.15.0 | Up 2m | 5001/tcp | MODE=worker ✅ |
| docker-worker_beat-1 | langgenius/dify-api:1.15.0 | Up 2m | 5001/tcp | MODE=beat ✅ |
| docker-api_websocket-1 | langgenius/dify-api:1.15.0 | Up 2m | 5001/tcp | collaboration profile ✅ |
| docker-db_postgres-1 | postgres:15-alpine | **Up 3m (healthy)** | 5432/tcp | 默认 DB ✅ |
| docker-redis-1 | redis:6-alpine | **Up 3m (healthy)** | 6379/tcp | 缓存+broker ✅ |
| docker-sandbox-1 | langgenius/dify-sandbox:0.2.15 | **Up 3m (healthy)** | 8194/tcp | 代码执行 ✅ |
| docker-plugin_daemon-1 | langgenius/dify-plugin-daemon:0.6.3-local | Up 2m | 0.0.0.0:5003→5003 | 插件运行时 ✅ |
| docker-ssrf_proxy-1 | ubuntu/squid:latest | Up 3m | 3128/tcp | 受限外网 ✅ |
| docker-weaviate-1 | semitechnologies/weaviate:1.27.0 | Up 3m | 8080/50051 | 默认向量库；`GET :8080/v1/.well-known/ready` = READY ✅ |

> 13 个容器全部 **Up**；无 `unhealthy`、无未启动。`api`/`db_postgres`/`redis`/`sandbox` 显式 healthy。weaviate 在主 compose 无 healthcheck 块，但容器内 `:8080` ready 已验证。

## 步骤 5 · 访问验证（初始化前）

| 验证方式 | 地址 | 结果 |
|---------|------|------|
| HTTP（curl） | `http://localhost/` | **307** → 重定向到 `/install`（nginx 正常） |
| HTTP（curl） | `http://localhost/install` | **200**（安装页返回） |
| HTTP（curl） | `http://localhost:5001/health` | **000**（api 端口未对宿主机发布，仅经 nginx 内部代理；非异常） |
| 真实浏览器 | `http://localhost/install` | **页面正常加载**，渲染出 Dify 初始化管理员账号页（"Setting up an admin account" + Email/Username/Password 表单，无 CAPTCHA）✅ |
| 截图证据 | `C:\Users\Nostal\AppData\Local\hermes\cache\screenshots\browser_screenshot_9d130bc7861e4fe5bb4720fd477543fa.png` | 已留存 |

> 说明：`localhost:5001/health` 返回 000 是因为 docker-compose.yaml 中 `api` 服务仅暴露 `5001/tcp`（不发布到宿主机），外部经 nginx(:80) 访问；api 容器自身 `health: healthy` 已证明服务可用。真正判定「启动成功」的依据是 `localhost/install`=200 且浏览器页面可加载，二者均满足。

## 步骤 5b · 初始化完成后的二次验证（用户已创建管理员账号）

| 验证方式 | 地址 / 命令 | 结果 |
|---------|------------|------|
| 初始化状态 API | `GET /console/api/setup` | `{"step":"finished","setup_at":"2026-07-15T03:33:44"}` ✅ 已完成 |
| HTTP（curl 跟随重定向） | `http://localhost/` → `-L` | final=**200**, url=`http://localhost/signin?redirect_url=%2F`（安装完成后跳登录）✅ |
| HTTP（curl） | `http://localhost/install` | 200（已初始化时重定向到 signin，符合预期） |
| api 内部健康 | `docker compose exec api curl :5001/health` | **200** ✅ |
| 真实浏览器 | `http://localhost/signin` | 渲染 **"Log in to Dify"** 页（Email + Password，"Welcome! Please log in to get started"）✅ |

> 结论：管理员账号已创建、初始化 finished，平台进入正常登录态。服务保持运行。

## 步骤 6 · 故障处理（本次）

- 阻塞点 1：Docker Desktop 未运行 → 启动它（可逆、安全，符合「最小修复」）。
- 阻塞点 2：`.env` 缺失导致 config 解析失败 → 从示例复制（最小、可逆）。
- **未执行** `down -v` / `prune` / 删 volumes / 重建库；未修改任何配置。
- 慢拉取（weaviate）按「持续观察、不重复启动」处理，最终成功。

## 关键日志结论

- `api` 最近日志：**无 error / exception / traceback** 行。
- `worker` 最近日志：**无 error / exception / traceback** 行。
- `weaviate` `:8080/v1/.well-known/ready` = READY。

## 仍待处理的问题

- **初始化未完成**：浏览器停在「设置管理员账号」页，需用户手动填写 Email/Username/Password 完成安装（这一步涉及写账号/密码，按边界规则不由 AI 代填）。
- 高质知识库索引需配置 Embedding 模型（经 Plugin Daemon 或内置 provider），属安装后配置，非启动阻塞。
- 服务**保持运行**，未主动停止。

## 给出后可复查的最小命令

```bash
cd C:\ai/RuyiDify\docker
docker compose ps                 # 看全部 Up / healthy
curl -s -o /dev/null -w "%{http_code}\n" http://localhost/install   # 期望 200
docker compose logs --tail=20 api # 看有无 error
```
